package com.noandish.nupdate.utils

import android.content.Context

class DebugUtils(val context : Context,val tag : String? = null, val message : String? = null )  {
    init {
        //TODO create file Debug if enabled debug in application
    }
}